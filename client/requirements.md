## Packages
framer-motion | Complex animations and transitions
lucide-react | Beautiful icons
clsx | Utility for conditional classes (standard with tailwind-merge)
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config: Extended colors for gold, mint, peach, lavender
Fonts: Outfit (Headings), DM Sans (Body)
Authentication: Custom hooks connecting to /api/login, /api/user, /api/logout
API: Using standard fetch with credentials: "include" via hooks
