{
  "name": "functions",
  "version": "1.0.0",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "esbuild --bundle --platform=node --target=node20 --format=cjs --outfile=lib/index.js src/index.ts --tsconfig=../tsconfig.json --external:firebase-admin --external:firebase-functions",
    "lint": "exit 0"
  },
  "dependencies": {
    "express": "^4.21.2",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^7.0.3",
    "rest-express": "file:.."
  },
  "devDependencies": {
    "esbuild": "^0.27.2",
    "eslint": "^8.15.0",
    "eslint-config-google": "^0.14.0",
    "eslint-plugin-import": "^2.26.0",
    "tsx": "^4.21.0",
    "typescript-eslint": "^8.53.0"
  },
  "private": true
}
